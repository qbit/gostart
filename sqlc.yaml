version: 2
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "queries.sql"
    gen:
      go:
        package: "data"
        out: "data"
        emit_json_tags: true
        emit_empty_slices: true
